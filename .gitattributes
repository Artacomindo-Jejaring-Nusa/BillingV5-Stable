# Auto detect text files and normalize line endings
* text=auto

# Force shell scripts to always use LF (Linux line endings)
*.sh text eol=lf

# Force batch/powershell to use CRLF (Windows)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# MySQL config
*.cnf text eol=lf
